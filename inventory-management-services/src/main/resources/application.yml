server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task_manager_db?createDatabaseIfNotExist=true
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update

  output:
    ansi:
      enabled: always